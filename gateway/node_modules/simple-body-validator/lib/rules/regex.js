'use strict';
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var ruleContract_1 = __importDefault(require("./ruleContract"));
var Regex = /** @class */ (function (_super) {
    __extends(Regex, _super);
    function Regex(regex, shouldMatch) {
        if (shouldMatch === void 0) { shouldMatch = true; }
        var _this = _super.call(this) || this;
        _this.regex = regex;
        _this.shouldMatch = shouldMatch;
        return _this;
    }
    Regex.prototype.passes = function (value) {
        if (this.shouldMatch) {
            return this.regex.test(value);
        }
        return !this.regex.test(value);
    };
    Regex.prototype.getMessage = function () {
        if (this.shouldMatch) {
            return this.trans('regex');
        }
        return this.trans('not_regex');
    };
    return Regex;
}(ruleContract_1.default));
exports.default = Regex;
